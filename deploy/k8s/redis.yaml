apiVersion: apps/v1
kind: Deployment
metadata:
  name: kvcache-redis
  namespace: kvcache-serve
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kvcache-redis
  template:
    metadata:
      labels:
        app: kvcache-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: kvcache-redis
  namespace: kvcache-serve
spec:
  selector:
    app: kvcache-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
